# Runtime dependencies for the deployed web app (Fly.io image).
#
# Mirrors requirements.txt but deliberately omits `sentence-transformers`,
# which drags in torch (~2.5GB). Torch is only reachable through a lazy
# `from sentence_transformers import SentenceTransformer` inside
# Pipelineteamfile/opportunity_classifier/mlfilter/embeddings.py - the Reflex
# app itself never imports it. Excluding it keeps the image small enough to
# build and run on a cheap Fly VM.
#
# Keep this file in sync with requirements.txt when adding web-app deps.
reflex>=0.9.8,<1
requests>=2.31,<3
PyYAML>=6.0.1,<7
python-dateutil>=2.9,<3
python-dotenv>=1.0,<2
openai>=1.0,<4
scikit-learn>=1.3,<2
pypdf>=5,<7
python-pptx>=1,<2
python-docx>=1,<2
beautifulsoup4>=4.12,<5
matplotlib>=3.8,<4
gunicorn>=23,<24
uvicorn>=0.30,<1
